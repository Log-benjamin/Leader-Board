const listScore = document.querySelector('.listNameAndScore');
const refreshBtn = document.querySelector('.refreshData');
const submitBtn = document.querySelector('.submitnewData');
const newName = document.querySelector('.inputName');
const newScore = document.querySelector('.inputScore');

export default listScore;
export {
  refreshBtn, submitBtn, newName, newScore,
};